import React from 'react';
import { Box, Typography } from '@mui/material';

const Home: React.FC = () => (
  <Box>
    <Typography variant="h4" gutterBottom>
      Каждому максимальный балл за эссе
    </Typography>
  </Box>
);

export default Home;
